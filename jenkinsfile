pipeline{
    agent any
    stages{
        stage("build"){
            steps{
                sh 'npm pack'
                sh 'npm run start &'
            }
        }
        stage("test"){
            steps{
                echo "====++++Testing++++===="
            }
        }
    }
}
